//! Sapphire Language Server.
//!
//! This crate will host the LSP implementation. At the scaffolding
//! stage (I2) it is intentionally empty; real wiring lands starting
//! with L0 (LSP crate selection) / L1 (protocol skeleton).

/// Placeholder to keep the crate compiling before real code lands.
pub fn placeholder() {}
