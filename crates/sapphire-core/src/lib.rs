//! Shared types for the Sapphire compiler and language server.
//!
//! This crate is intentionally empty at the scaffolding stage (I2).
//! Horizontal types (spans, source ids, diagnostics, and eventually
//! the AST / type representation) will move in here as later tasks
//! (I3+, L0+) need to share them between `sapphire-compiler` and
//! `sapphire-lsp`.

/// Placeholder to keep the crate compiling before real code lands.
pub fn placeholder() {}
