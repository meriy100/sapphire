# Bundler-local state (this gem is published as a library;
# development repo intentionally does not pin a Gemfile.lock).
# Rationale: docs/impl/08-runtime-layout.md §Bundler 使用.
/.bundle/
/vendor/bundle/
/Gemfile.lock

# Built gem artifacts from `gem build`.
/*.gem
/pkg/
